# config.py

# Board is in GRID CELLS (not pixels)
GRID_WIDTH = 20
GRID_HEIGHT = 20
CELL_SIZE = 24  # pixels per grid cell

# Simulation speed (ticks per second)
TICKS_PER_SECOND = 10

# Render speed (frames per second)
FPS = 60

# UI
HUD_HEIGHT = 50
WINDOW_TITLE = "Snake (Clean Architecture)"
